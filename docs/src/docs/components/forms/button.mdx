---
title: Button
description: Interactive button component
sidebar_position: 2
---

import CodeBlock from '@/components/mdx/CodeBlock.astro';


# Button

Trigger actions and events with interactive buttons.

## Basic Usage

<CodeBlock lang="json">
```json
{
  "type": "button",
  "text": "Click Me",
  "on_click": [
    { "type": "update_state", "path": "clicked", "value": true }
  ]
}
```
</CodeBlock>

## Properties

- `text` - Button label (supports expressions like `${state.label}`)
- `on_click` - Array of actions to execute on click
- `variant` - Visual style (default, primary, secondary, outline, destructive, ghost)
- `size` - Button size (default, sm, lg, icon)
- `disabled` - Disable button interaction (supports expressions)
- `icon` - Optional icon name
- `loading` - Show loading spinner

## Examples

### Primary Button

<CodeBlock lang="json">
```json
{
  "type": "button",
  "text": "Save Changes",
  "variant": "primary",
  "on_click": [
    { "type": "call_api", "api": "save_data", "payload": "${state.formData}" }
  ]
}
```
</CodeBlock>

### Disabled State

<CodeBlock lang="json">
```json
{
  "type": "button",
  "text": "Submit",
  "disabled": "${!state.isValid}",
  "on_click": []
}
```
</CodeBlock>

### With Icon

<CodeBlock lang="json">
```json
{
  "type": "button",
  "text": "Delete",
  "variant": "destructive",
  "icon": "trash",
  "on_click": [
    { "type": "show_toast", "message": "Item deleted" }
  ]
}
```
</CodeBlock>

### Loading State

<CodeBlock lang="json">
```json
{
  "type": "button",
  "text": "Processing...",
  "loading": "${state.isProcessing}",
  "disabled": "${state.isProcessing}",
  "on_click": []
}
```
</CodeBlock>
